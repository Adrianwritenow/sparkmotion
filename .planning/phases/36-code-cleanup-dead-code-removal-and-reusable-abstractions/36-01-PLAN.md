---
phase: 36-code-cleanup-dead-code-removal-and-reusable-abstractions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/ui/package.json
  - packages/ui/src/index.ts
  - packages/ui/src/components/ui/badge.tsx
  - packages/ui/src/components/ui/button.tsx
  - packages/ui/src/components/ui/calendar.tsx
  - packages/ui/src/components/ui/card.tsx
  - packages/ui/src/components/ui/chart.tsx
  - packages/ui/src/components/ui/checkbox.tsx
  - packages/ui/src/components/ui/command.tsx
  - packages/ui/src/components/ui/dialog.tsx
  - packages/ui/src/components/ui/dropdown-menu.tsx
  - packages/ui/src/components/ui/form.tsx
  - packages/ui/src/components/ui/input.tsx
  - packages/ui/src/components/ui/label.tsx
  - packages/ui/src/components/ui/popover.tsx
  - packages/ui/src/components/ui/select.tsx
  - packages/ui/src/components/ui/sheet.tsx
  - packages/ui/src/components/ui/skeleton.tsx
  - packages/ui/src/components/ui/switch.tsx
  - packages/ui/src/components/ui/table.tsx
  - packages/ui/src/components/ui/tabs.tsx
  - packages/ui/src/components/ui/tooltip.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "All 20 shadcn primitives exist in packages/ui/src/components/ui/ and export correctly"
    - "Both apps import all shadcn components from @sparkmotion/ui/* subpath exports (zero local copies remain)"
    - "pnpm build succeeds for both admin and customer apps with zero type errors from the migration"
    - "packages/ui/package.json declares all Radix UI and peer deps that shadcn components require"
  artifacts:
    - path: "packages/ui/src/components/ui/button.tsx"
      provides: "Button component with cn import from ../../lib/utils"
      contains: "import { cn } from"
    - path: "packages/ui/package.json"
      provides: "All Radix UI dependencies for shadcn components"
      contains: "@radix-ui/react-dialog"
    - path: "packages/ui/src/index.ts"
      provides: "Re-exports for all 20 shadcn primitives"
      min_lines: 20
  key_links:
    - from: "apps/admin/src/**/*.tsx"
      to: "@sparkmotion/ui/*"
      via: "subpath import"
      pattern: 'from "@sparkmotion/ui/'
    - from: "apps/customer/src/**/*.tsx"
      to: "@sparkmotion/ui/*"
      via: "subpath import"
      pattern: 'from "@sparkmotion/ui/'
---

<objective>
Migrate all 20 shadcn UI primitives from both apps into packages/ui as the single source of truth, update all ~413 import sites across admin (239) and customer (174) apps to use @sparkmotion/ui/* subpath exports, and delete the local copies.

Purpose: Eliminate 40 duplicated files (20 per app) that must be manually kept in sync. Establish packages/ui as the canonical location for all shared UI components per locked decision.

Output: packages/ui contains all 20 shadcn primitives with correct deps; both apps import exclusively from @sparkmotion/ui/*; zero local component/ui/ files remain in either app.
</objective>

<execution_context>
@/Users/adrianrodriguez/.claude/get-shit-done/workflows/execute-plan.md
@/Users/adrianrodriguez/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-code-cleanup-dead-code-removal-and-reusable-abstractions/36-RESEARCH.md

<interfaces>
<!-- Current packages/ui structure -->
packages/ui/src/
  components/ui/chart.tsx  (already exists — use customer version as canonical, see Pitfall 2)
  lib/utils.ts             (cn() already here)
  index.ts                 (currently only exports chart + cn)

<!-- packages/ui/package.json currently has: -->
dependencies: react, clsx, tailwind-merge, recharts
<!-- Missing: all @radix-ui/*, class-variance-authority, cmdk, etc. -->

<!-- Both apps already have in next.config.mjs: -->
transpilePackages: ["@sparkmotion/api", "@sparkmotion/database", "@sparkmotion/ui", "@sparkmotion/auth"]

<!-- CLAUDE.md convention for shadcn imports: -->
import { Button } from '@sparkmotion/ui/button'
import { Card, CardHeader, CardContent } from '@sparkmotion/ui/card'
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add dependencies to packages/ui and copy shadcn primitives with cn path fix</name>
  <files>
    packages/ui/package.json
    packages/ui/src/components/ui/badge.tsx
    packages/ui/src/components/ui/button.tsx
    packages/ui/src/components/ui/calendar.tsx
    packages/ui/src/components/ui/card.tsx
    packages/ui/src/components/ui/chart.tsx
    packages/ui/src/components/ui/checkbox.tsx
    packages/ui/src/components/ui/command.tsx
    packages/ui/src/components/ui/dialog.tsx
    packages/ui/src/components/ui/dropdown-menu.tsx
    packages/ui/src/components/ui/form.tsx
    packages/ui/src/components/ui/input.tsx
    packages/ui/src/components/ui/label.tsx
    packages/ui/src/components/ui/popover.tsx
    packages/ui/src/components/ui/select.tsx
    packages/ui/src/components/ui/sheet.tsx
    packages/ui/src/components/ui/skeleton.tsx
    packages/ui/src/components/ui/switch.tsx
    packages/ui/src/components/ui/table.tsx
    packages/ui/src/components/ui/tabs.tsx
    packages/ui/src/components/ui/tooltip.tsx
    packages/ui/src/index.ts
  </files>
  <action>
    **Step 1 — Add dependencies to packages/ui/package.json:**

    Run `pnpm add --filter @sparkmotion/ui` for these dependencies (use exact versions from apps/admin/package.json to avoid version mismatches — see Pitfall 4):
    - @radix-ui/react-checkbox ^1.3.3
    - @radix-ui/react-dialog ^1.1.15
    - @radix-ui/react-dropdown-menu ^2.1.16
    - @radix-ui/react-label ^2.1.8
    - @radix-ui/react-popover ^1.1.15
    - @radix-ui/react-select ^2.2.6
    - @radix-ui/react-slot ^1.2.4
    - @radix-ui/react-switch ^1.2.6
    - @radix-ui/react-tabs ^1.1.13
    - @radix-ui/react-tooltip ^1.2.8
    - class-variance-authority ^0.7.1
    - cmdk ^1.1.1
    - date-fns ^4.1.0
    - @date-fns/tz (check version in apps/admin/package.json)
    - lucide-react ^0.563.0
    - react-day-picker ^9.13.2
    - react-hook-form ^7.71.1
    - @hookform/resolvers (check version in apps/admin/package.json)
    - zod ^3.25.76

    Also add as peerDependencies: react-dom (same version as react peer).

    NOTE: Do NOT add sonner, use-places-autocomplete, or next — those are only needed by business components (Plan 02) or the apps themselves.

    **Step 2 — Copy all 20 shadcn primitives into packages/ui/src/components/ui/:**

    For ALL components EXCEPT chart.tsx: copy from `apps/admin/src/components/ui/` to `packages/ui/src/components/ui/`. These are confirmed identical between apps (per RESEARCH.md).

    For chart.tsx specifically: Use the CUSTOMER version (`apps/customer/src/components/ui/chart.tsx`, 380 lines) as the canonical version because it has a superset API (expanded ChartTooltipContent with labelFormatter, formatter props). The admin version (369 lines) is already in packages/ui — REPLACE it with the customer version. This is backwards-compatible since admin only uses a subset of props.

    **Step 3 — Fix cn import paths in ALL copied files:**

    Every shadcn component currently has: `import { cn } from "@/lib/utils"`
    Change to: `import { cn } from "../../lib/utils"`
    This is the ONLY code change inside the component files.

    The relative path `../../lib/utils` is correct because components live at `src/components/ui/X.tsx` and utils is at `src/lib/utils.ts`.

    **Step 4 — Add subpath exports to packages/ui/package.json:**

    Add to the `"exports"` field (keeping existing `.` and `./assets` entries):
    ```json
    "./badge": "./src/components/ui/badge.tsx",
    "./button": "./src/components/ui/button.tsx",
    "./calendar": "./src/components/ui/calendar.tsx",
    "./card": "./src/components/ui/card.tsx",
    "./chart": "./src/components/ui/chart.tsx",
    "./checkbox": "./src/components/ui/checkbox.tsx",
    "./command": "./src/components/ui/command.tsx",
    "./dialog": "./src/components/ui/dialog.tsx",
    "./dropdown-menu": "./src/components/ui/dropdown-menu.tsx",
    "./form": "./src/components/ui/form.tsx",
    "./input": "./src/components/ui/input.tsx",
    "./label": "./src/components/ui/label.tsx",
    "./popover": "./src/components/ui/popover.tsx",
    "./select": "./src/components/ui/select.tsx",
    "./sheet": "./src/components/ui/sheet.tsx",
    "./skeleton": "./src/components/ui/skeleton.tsx",
    "./switch": "./src/components/ui/switch.tsx",
    "./table": "./src/components/ui/table.tsx",
    "./tabs": "./src/components/ui/tabs.tsx",
    "./tooltip": "./src/components/ui/tooltip.tsx"
    ```

    **Step 5 — Update packages/ui/src/index.ts:**

    Add re-exports for all 20 primitives (in addition to existing chart + cn exports). Example:
    ```typescript
    export * from "./components/ui/badge";
    export * from "./components/ui/button";
    // ... all 20
    ```

    Remove the existing duplicate chart re-export lines and consolidate into the alphabetical list.

    **Step 6 — Run `pnpm install` from repo root** to update the lockfile.
  </action>
  <verify>
    <automated>cd /Users/adrianrodriguez/Desktop/sparkmotion && ls packages/ui/src/components/ui/*.tsx | wc -l | grep -q "20" && grep -c '@radix-ui' packages/ui/package.json | grep -q '[5-9]\|1[0-9]' && echo "PASS: 20 components + radix deps present"</automated>
  </verify>
  <done>
    - 20 shadcn component files exist in packages/ui/src/components/ui/
    - All use `import { cn } from "../../lib/utils"` (not `@/lib/utils`)
    - packages/ui/package.json has all required Radix UI + CVA + cmdk dependencies
    - packages/ui/package.json has 20 subpath exports for each component
    - packages/ui/src/index.ts re-exports all 20 primitives
    - chart.tsx is the customer version (380 lines, superset API)
  </done>
</task>

<task type="auto">
  <name>Task 2: Update all import sites in both apps and delete local copies</name>
  <files>
    apps/admin/src/components/ui/ (DELETE all 20 files)
    apps/customer/src/components/ui/ (DELETE all 20 files)
    apps/admin/src/**/*.tsx (update ~239 import sites)
    apps/customer/src/**/*.tsx (update ~174 import sites)
  </files>
  <action>
    **Step 1 — Bulk find-and-replace import paths in admin app:**

    In ALL .tsx and .ts files under `apps/admin/src/`, replace:
    `from "@/components/ui/badge"` → `from "@sparkmotion/ui/badge"`
    `from "@/components/ui/button"` → `from "@sparkmotion/ui/button"`
    `from "@/components/ui/calendar"` → `from "@sparkmotion/ui/calendar"`
    `from "@/components/ui/card"` → `from "@sparkmotion/ui/card"`
    `from "@/components/ui/chart"` → `from "@sparkmotion/ui/chart"`
    `from "@/components/ui/checkbox"` → `from "@sparkmotion/ui/checkbox"`
    `from "@/components/ui/command"` → `from "@sparkmotion/ui/command"`
    `from "@/components/ui/dialog"` → `from "@sparkmotion/ui/dialog"`
    `from "@/components/ui/dropdown-menu"` → `from "@sparkmotion/ui/dropdown-menu"`
    `from "@/components/ui/form"` → `from "@sparkmotion/ui/form"`
    `from "@/components/ui/input"` → `from "@sparkmotion/ui/input"`
    `from "@/components/ui/label"` → `from "@sparkmotion/ui/label"`
    `from "@/components/ui/popover"` → `from "@sparkmotion/ui/popover"`
    `from "@/components/ui/select"` → `from "@sparkmotion/ui/select"`
    `from "@/components/ui/sheet"` → `from "@sparkmotion/ui/sheet"`
    `from "@/components/ui/skeleton"` → `from "@sparkmotion/ui/skeleton"`
    `from "@/components/ui/switch"` → `from "@sparkmotion/ui/switch"`
    `from "@/components/ui/table"` → `from "@sparkmotion/ui/table"`
    `from "@/components/ui/tabs"` → `from "@sparkmotion/ui/tabs"`
    `from "@/components/ui/tooltip"` → `from "@sparkmotion/ui/tooltip"`

    Use sed or a scripted approach for efficiency. The pattern is deterministic: replace `@/components/ui/X` with `@sparkmotion/ui/X` for each of the 20 component names.

    **Step 2 — Repeat the same bulk replacement for customer app:**

    Same pattern in all .tsx and .ts files under `apps/customer/src/`.

    **Step 3 — Delete local shadcn copies from both apps:**

    Delete the entire `apps/admin/src/components/ui/` directory (all 20 files).
    Delete the entire `apps/customer/src/components/ui/` directory (all 20 files).

    **Step 4 — Also update any `@/lib/utils` imports that reference `cn`:**

    Check if any admin/customer component files import `cn` from `@/lib/utils`. These should now import from `@sparkmotion/ui` instead:
    `import { cn } from "@/lib/utils"` → `import { cn } from "@sparkmotion/ui"`

    HOWEVER: Only update files that ONLY use `cn` from `@/lib/utils`. If `apps/*/src/lib/utils.ts` has other exports used by the app, leave those imports alone. The apps' own `utils.ts` files likely only export `cn`, so check first: if `apps/admin/src/lib/utils.ts` only contains the `cn` function (same as packages/ui), then update all cn imports to use the package AND delete the local utils.ts. If it has app-specific utilities, leave it.

    **Step 5 — Verify no remaining references to `@/components/ui/`:**

    Run: `grep -rn 'from "@/components/ui/' apps/admin/src apps/customer/src --include="*.tsx" --include="*.ts"`
    This MUST return zero results.

    **Step 6 — Build both apps to verify:**

    Run: `pnpm --filter @sparkmotion/admin build` and `pnpm --filter @sparkmotion/customer build`
    Both must succeed without errors. TypeScript will catch any missed import sites or incorrect paths.
  </action>
  <verify>
    <automated>cd /Users/adrianrodriguez/Desktop/sparkmotion && grep -rn 'from "@/components/ui/' apps/admin/src apps/customer/src --include="*.tsx" --include="*.ts" | wc -l | grep -q "^0$" && echo "PASS: zero local ui imports remain"</automated>
  </verify>
  <done>
    - Zero files remain in apps/admin/src/components/ui/ and apps/customer/src/components/ui/
    - Zero import sites reference `@/components/ui/` in either app
    - All ~413 import sites now reference `@sparkmotion/ui/*` subpath exports
    - Both apps build successfully with `pnpm build`
  </done>
</task>

</tasks>

<verification>
1. `ls packages/ui/src/components/ui/*.tsx | wc -l` returns 20
2. `grep -rn 'from "@/components/ui/' apps/ --include="*.tsx" --include="*.ts" | wc -l` returns 0
3. `ls apps/admin/src/components/ui/ 2>/dev/null` returns error (directory deleted)
4. `ls apps/customer/src/components/ui/ 2>/dev/null` returns error (directory deleted)
5. `pnpm --filter @sparkmotion/admin build` succeeds
6. `pnpm --filter @sparkmotion/customer build` succeeds
</verification>

<success_criteria>
- All 20 shadcn primitives consolidated into packages/ui as single source of truth
- Both apps exclusively import from @sparkmotion/ui/* subpath exports
- Zero local component/ui/ copies remain in either app
- Both apps build and type-check successfully
- chart.tsx uses customer superset version (380 lines) per Pitfall 2
</success_criteria>

<output>
After completion, create `.planning/phases/36-code-cleanup-dead-code-removal-and-reusable-abstractions/36-01-SUMMARY.md`
</output>
