---
phase: 39-refactor-shared-abstractions
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/ui/src/components/layout/sidebar.tsx
  - packages/ui/src/components/layout/mobile-header.tsx
  - packages/ui/src/components/layout/index.ts
  - packages/ui/package.json
  - apps/admin/src/components/layout/sidebar.tsx
  - apps/admin/src/components/layout/mobile-header.tsx
  - apps/admin/src/app/(dashboard)/layout.tsx
  - apps/customer/src/components/layout/sidebar.tsx
  - apps/customer/src/components/layout/mobile-header.tsx
  - apps/customer/src/app/(dashboard)/layout.tsx
  - apps/admin/src/components/sort-row.tsx
  - apps/customer/src/components/sort-row.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "Shared Sidebar component accepts navItems array prop — apps configure their own navigation"
    - "Sidebar does NOT call tRPC directly — app layouts compute showAlert and pass it via navItems"
    - "MobileHeader is a single shared component in packages/ui"
    - "sort-row local copies in both apps are deleted (packages/ui version already exists)"
  artifacts:
    - path: "packages/ui/src/components/layout/sidebar.tsx"
      provides: "Config-driven Sidebar with navItems prop"
    - path: "packages/ui/src/components/layout/mobile-header.tsx"
      provides: "Shared MobileHeader component"
    - path: "packages/ui/src/components/layout/index.ts"
      provides: "Barrel export for layout domain"
  key_links:
    - from: "apps/admin/src/app/(dashboard)/layout.tsx"
      to: "packages/ui/src/components/layout/sidebar.tsx"
      via: "import Sidebar from @sparkmotion/ui/layout, pass adminNavItems"
      pattern: "from.*@sparkmotion/ui/layout"
    - from: "apps/customer/src/app/(dashboard)/layout.tsx"
      to: "packages/ui/src/components/layout/sidebar.tsx"
      via: "import Sidebar from @sparkmotion/ui/layout, pass customerNavItems"
      pattern: "from.*@sparkmotion/ui/layout"
---

<objective>
Extract layout components (Sidebar, MobileHeader) into packages/ui with config-driven navItems pattern, and clean up stale sort-row copies.

Purpose: The sidebar is identical in rendering logic between apps — only navigation items differ. Making it config-driven eliminates the largest layout duplication. Sort-row already exists in packages/ui but both apps still have local copies.
Output: Config-driven Sidebar and MobileHeader in packages/ui/layout, sort-row local copies deleted.
</objective>

<execution_context>
@/Users/adrianrodriguez/.claude/get-shit-done/workflows/execute-plan.md
@/Users/adrianrodriguez/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-refactor-shared-abstractions-extract-reusable-functions-hooks-and-components-across-apps/39-CONTEXT.md
@.planning/phases/39-refactor-shared-abstractions-extract-reusable-functions-hooks-and-components-across-apps/39-RESEARCH.md
@packages/ui/package.json
@apps/admin/src/components/layout/sidebar.tsx
@apps/customer/src/components/layout/sidebar.tsx
@apps/admin/src/components/layout/mobile-header.tsx
@apps/customer/src/components/layout/mobile-header.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create config-driven Sidebar and shared MobileHeader in packages/ui</name>
  <files>
    packages/ui/src/components/layout/sidebar.tsx
    packages/ui/src/components/layout/mobile-header.tsx
    packages/ui/src/components/layout/index.ts
    packages/ui/package.json
  </files>
  <action>
**Sidebar refactor:**
1. Read both apps/admin/src/components/layout/sidebar.tsx and apps/customer/src/components/layout/sidebar.tsx.
2. Diff them. The rendering logic (collapse/expand, theme toggle, user profile, mobile Sheet drawer, tablet icon-rail) should be identical. The differences are:
   - Navigation items array (admin has organizations, users, change-logs, usage; customer has fewer items)
   - Admin sidebar may have `trpc.bands.flaggedCount.useQuery()` inline for showing an alert badge on one nav item
3. Create packages/ui/src/components/layout/sidebar.tsx that:
   - Accepts `navItems: NavItem[]` prop where NavItem = `{ href: string; label: string; icon: React.ElementType; showAlert?: boolean }`
   - Accepts `isMobile?: boolean` prop (existing pattern from Phase 20)
   - Does NOT call tRPC directly — the `showAlert` property is pre-computed by the app layout
   - Renders the same collapse/expand, theme toggle, user profile, mobile Sheet drawer, and tablet icon-rail logic
   - Uses `usePathname()` from next/navigation for active state highlighting
4. Check what auth/session info the sidebar displays (user name, email). If it reads `useSession()`, accept a `user?: { name: string; email: string; image?: string }` prop instead, letting the app layout pass session data.

**MobileHeader:**
1. Read both app versions — should be functionally identical.
2. Move to packages/ui/src/components/layout/mobile-header.tsx.
3. Accept props for any app-specific content (logo, menu toggle callback).

**Barrel and exports:**
- Create packages/ui/src/components/layout/index.ts exporting Sidebar, MobileHeader, and the NavItem type.
- Add `"./layout": "./src/components/layout/index.ts"` to packages/ui/package.json exports.
  </action>
  <verify>
    <automated>cd /Users/adrianrodriguez/Desktop/sparkmotion && ls packages/ui/src/components/layout/ && grep '"./layout"' packages/ui/package.json</automated>
  </verify>
  <done>Config-driven Sidebar with navItems prop and shared MobileHeader exist in packages/ui/src/components/layout/ with barrel export and package.json entry.</done>
</task>

<task type="auto">
  <name>Task 2: Update app layouts to use shared Sidebar, clean up sort-row, verify build</name>
  <files>
    apps/admin/src/app/(dashboard)/layout.tsx
    apps/customer/src/app/(dashboard)/layout.tsx
    apps/admin/src/components/layout/sidebar.tsx (DELETE)
    apps/admin/src/components/layout/mobile-header.tsx (DELETE)
    apps/customer/src/components/layout/sidebar.tsx (DELETE)
    apps/customer/src/components/layout/mobile-header.tsx (DELETE)
    apps/admin/src/components/sort-row.tsx (DELETE)
    apps/customer/src/components/sort-row.tsx (DELETE)
  </files>
  <action>
**Admin layout update:**
1. Read apps/admin/src/app/(dashboard)/layout.tsx.
2. Import Sidebar and MobileHeader from `@sparkmotion/ui/layout`.
3. Define `adminNavItems` array with all admin navigation items (Dashboard, Organizations, Events, Campaigns, Activity, Users, Change Logs, Usage).
4. If the layout currently calls `trpc.bands.flaggedCount.useQuery()`, keep that call in the layout and inject `showAlert: (flaggedCount ?? 0) > 0` into the relevant navItem entry.
5. Pass `navItems={adminNavItems}` and `user` session data to Sidebar.

**Customer layout update:**
1. Read apps/customer/src/app/(dashboard)/layout.tsx.
2. Import Sidebar and MobileHeader from `@sparkmotion/ui/layout`.
3. Define `customerNavItems` array with customer navigation items (Dashboard, Events, Campaigns, Activity).
4. Pass `navItems={customerNavItems}` and `user` session data to Sidebar.

**Delete local copies:**
- Delete apps/admin/src/components/layout/sidebar.tsx
- Delete apps/admin/src/components/layout/mobile-header.tsx
- Delete apps/customer/src/components/layout/sidebar.tsx
- Delete apps/customer/src/components/layout/mobile-header.tsx
- If layout directories become empty, delete them

**Sort-row cleanup:**
1. Check if apps/admin/src/components/sort-row.tsx and apps/customer/src/components/sort-row.tsx are still imported anywhere.
2. If apps import from local `@/components/sort-row`, update to `import { SortRow } from "@sparkmotion/ui"` (it already exists in packages/ui/src/components/sort-row.tsx).
3. Delete both local sort-row.tsx copies.

**Verify:**
- Run `pnpm build` to confirm everything compiles.
- Grep for any remaining imports from `@/components/layout/` or `@/components/sort-row` in both apps.
  </action>
  <verify>
    <automated>cd /Users/adrianrodriguez/Desktop/sparkmotion && grep -r "from.*@/components/layout/\|from.*@/components/sort-row" apps/admin/src apps/customer/src --include="*.tsx" --include="*.ts" 2>/dev/null; echo "---"; pnpm build 2>&1 | tail -10</automated>
  </verify>
  <done>Both app layouts use shared Sidebar with navItems config. No local layout or sort-row copies remain. pnpm build succeeds.</done>
</task>

</tasks>

<verification>
- `grep -r "from.*@/components/layout/" apps/ --include="*.tsx"` returns zero results
- `grep -r "from.*@/components/sort-row" apps/ --include="*.tsx"` returns zero results
- Admin sidebar shows all admin nav items with correct active states
- Customer sidebar shows customer-specific nav items
- pnpm build succeeds
</verification>

<success_criteria>
- Sidebar is config-driven via navItems prop, lives in packages/ui/layout
- MobileHeader is shared in packages/ui/layout
- App layouts define their own navItems arrays
- Sidebar does not call tRPC directly
- Sort-row local copies deleted
- Build passes
</success_criteria>

<output>
After completion, create `.planning/phases/39-refactor-shared-abstractions-extract-reusable-functions-hooks-and-components-across-apps/39-03-SUMMARY.md`
</output>
