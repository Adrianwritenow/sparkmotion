---
phase: 39-refactor-shared-abstractions
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/api/src/lib/auth.ts
  - packages/api/src/routers/events.ts
  - packages/api/src/routers/campaigns.ts
  - packages/api/src/routers/bands.ts
  - packages/api/src/routers/analytics.ts
  - packages/api/src/routers/windows.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "getOrgFilter helper exists in packages/api/src/lib/auth.ts alongside enforceOrgAccess"
    - "All inline org-scoping patterns in events, campaigns, bands routers replaced with getOrgFilter calls"
    - "Any other 3+ repeated patterns across routers are extracted to shared helpers"
    - "All existing tests still pass after refactoring"
  artifacts:
    - path: "packages/api/src/lib/auth.ts"
      provides: "getOrgFilter helper + existing enforceOrgAccess"
      exports: ["enforceOrgAccess", "getOrgFilter"]
  key_links:
    - from: "packages/api/src/routers/events.ts"
      to: "packages/api/src/lib/auth.ts"
      via: "import { getOrgFilter } from '../lib/auth'"
      pattern: "getOrgFilter"
    - from: "packages/api/src/routers/campaigns.ts"
      to: "packages/api/src/lib/auth.ts"
      via: "import { getOrgFilter } from '../lib/auth'"
      pattern: "getOrgFilter"
---

<objective>
Extract getOrgFilter helper and audit all tRPC routers for repeated patterns worth extracting.

Purpose: The inline org-scoping pattern (`ctx.user.role === "ADMIN" ? input.orgId : ctx.user.orgId`) appears 10-15 times across 4 router files. Extracting it to a shared helper reduces boilerplate and ensures consistent org-scoping logic. The router audit identifies any other 3+ repeated patterns worth extracting.
Output: getOrgFilter in packages/api/src/lib/auth.ts, all routers refactored to use it, any additional extracted helpers.
</objective>

<execution_context>
@/Users/adrianrodriguez/.claude/get-shit-done/workflows/execute-plan.md
@/Users/adrianrodriguez/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-refactor-shared-abstractions-extract-reusable-functions-hooks-and-components-across-apps/39-CONTEXT.md
@.planning/phases/39-refactor-shared-abstractions-extract-reusable-functions-hooks-and-components-across-apps/39-RESEARCH.md
@packages/api/src/lib/auth.ts

<interfaces>
From packages/api/src/lib/auth.ts:
```typescript
export function enforceOrgAccess(
  ctx: { user: { role: string; orgId?: string | null } | null },
  entityOrgId: string
): void;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add getOrgFilter helper and refactor events + campaigns routers</name>
  <files>
    packages/api/src/lib/auth.ts
    packages/api/src/routers/events.ts
    packages/api/src/routers/campaigns.ts
  </files>
  <action>
1. Add getOrgFilter to packages/api/src/lib/auth.ts (alongside existing enforceOrgAccess):

```typescript
export function getOrgFilter(
  ctx: { user: { role: string; orgId?: string | null } | null },
  inputOrgId?: string
): { orgId?: string } {
  if (!ctx.user) throw new TRPCError({ code: "UNAUTHORIZED" });
  if (ctx.user.role === "CUSTOMER") {
    return { orgId: ctx.user.orgId ?? undefined };
  }
  return inputOrgId ? { orgId: inputOrgId } : {};
}
```

2. In packages/api/src/routers/events.ts:
   - Import `getOrgFilter` from `../lib/auth`
   - Find ALL instances of the inline org-scoping pattern (at least: events.list, events.listIds, and any other procedures that build a `where` clause with org filtering)
   - Replace each with: `const orgFilter = getOrgFilter(ctx, input?.orgId);` then spread into the where clause
   - Verify the replacement is semantically identical for each site

3. In packages/api/src/routers/campaigns.ts:
   - Same process: import getOrgFilter, find inline org-scoping, replace
   - At least: campaigns.list, campaigns.listIds

4. Do NOT change any behavior — this is a pure refactor. The returned Prisma where clause must produce identical queries.

5. Run existing tests to ensure nothing breaks: `pnpm test` (or `pnpm --filter @sparkmotion/api test`).
  </action>
  <verify>
    <automated>cd /Users/adrianrodriguez/Desktop/sparkmotion && grep -c "getOrgFilter" packages/api/src/routers/events.ts packages/api/src/routers/campaigns.ts packages/api/src/lib/auth.ts && pnpm --filter @sparkmotion/api test 2>&1 | tail -20</automated>
  </verify>
  <done>getOrgFilter exists in auth.ts. Events and campaigns routers use getOrgFilter instead of inline org-scoping. Tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor bands, analytics, windows routers + audit for additional patterns</name>
  <files>
    packages/api/src/routers/bands.ts
    packages/api/src/routers/analytics.ts
    packages/api/src/routers/windows.ts
  </files>
  <action>
1. In packages/api/src/routers/bands.ts:
   - Import `getOrgFilter` from `../lib/auth`
   - Find and replace inline org-scoping patterns with getOrgFilter
   - Bands router has 21 occurrences of role/orgId checks — many may be enforceOrgAccess calls (entity-level checks), not list-level org filters. Only replace the LIST-level patterns (where clauses for findMany/count) with getOrgFilter. Leave entity-level enforceOrgAccess calls as-is.

2. In packages/api/src/routers/analytics.ts:
   - This router is 1,937 lines with 35 role/orgId occurrences. The pattern here uses `eventWhere.orgId` assigned conditionally — slightly different from the simple object spread.
   - Audit carefully. If the pattern can be cleanly replaced with getOrgFilter (e.g., `const eventWhere = { ...getOrgFilter(ctx, input?.orgId) }`), do it. If the analytics router has a more complex layered pattern (eventId + orgId combined), leave it alone or extract a domain-specific helper — use your judgment per Claude's discretion.
   - Do NOT force getOrgFilter where it doesn't cleanly fit.

3. In packages/api/src/routers/windows.ts:
   - 4 occurrences — likely small. Apply getOrgFilter where appropriate.

4. **Full router audit:** Read through ALL router files and identify any OTHER pattern that appears 3+ times and is worth extracting:
   - Common include clauses (e.g., `include: { _count: { select: { bands: true } } }`)
   - Pagination patterns
   - Date filtering patterns
   - Sorting patterns
   - If any pattern appears 3+ times and saves meaningful code (not just 1-2 lines), extract to a helper in packages/api/src/lib/. Use your judgment — not every 3-line repeat needs extraction (KISS).

5. Run full test suite to verify nothing breaks.
  </action>
  <verify>
    <automated>cd /Users/adrianrodriguez/Desktop/sparkmotion && grep -c "getOrgFilter" packages/api/src/routers/bands.ts packages/api/src/routers/analytics.ts packages/api/src/routers/windows.ts && pnpm --filter @sparkmotion/api test 2>&1 | tail -20</automated>
  </verify>
  <done>Bands, analytics (where appropriate), and windows routers refactored. Router audit complete — any additional extracted helpers documented. All tests pass.</done>
</task>

</tasks>

<verification>
- `grep -c "getOrgFilter" packages/api/src/routers/*.ts` shows usage across routers
- `grep -c "ctx.user.role.*ADMIN.*input.*orgId\|ctx.user.role.*CUSTOMER" packages/api/src/routers/*.ts` shows reduced inline patterns
- `pnpm test` passes (all router tests green)
- No behavioral changes — pure refactor
</verification>

<success_criteria>
- getOrgFilter helper exists in packages/api/src/lib/auth.ts
- Events, campaigns, bands, windows routers use getOrgFilter for list-level org scoping
- Analytics router uses getOrgFilter where it cleanly fits
- Any additional 3+ repeated patterns extracted per audit
- All existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/39-refactor-shared-abstractions-extract-reusable-functions-hooks-and-components-across-apps/39-04-SUMMARY.md`
</output>
