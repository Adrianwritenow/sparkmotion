# Worker redirect endpoint
WORKER_URL=https://sparkmotion-redirect.xxx.workers.dev

# Cloudflare KV (for seeding test bands)
CF_ACCOUNT_ID=
CF_API_TOKEN=
CF_KV_NAMESPACE_ID=

# Upstash Redis REST (for pipeline isolation test)
UPSTASH_REDIS_REST_URL=https://xxx.upstash.io
UPSTASH_REDIS_REST_TOKEN=

# ioredis (for flush-taps benchmark + seeding)
REDIS_URL=redis://default:xxx@xxx.upstash.io:6379

# Hub (for triggering flush-taps cron + hub redirect tests)
HUB_URL=https://geo.sparkmotion.net
CRON_SECRET=

# Admin (for authenticated dashboard/export tests)
ADMIN_URL=https://admin-staging.sparkmotion.net

# Loadtest admin user credentials (for analytics + CSV export tests)
TEST_EMAIL=loadtest@sparkmotion.net
TEST_PASSWORD=

# Event ID from seeded data (CUID from Event table — run seed.ts postgres first)
LOADTEST_EVENT_ID=

# PostgreSQL (for seeding + query benchmarks)
DATABASE_URL=postgresql://user:pass@host:5432/db?sslmode=require

# Grafana Cloud k6 (optional — streams results to cloud dashboard)
K6_CLOUD_TOKEN=
