version: 2
updates:
  # Only keep GH Actions updated (small attack surface)
  # npm security updates handled by Dependabot Security Alerts (repo settings)
  - package-ecosystem: "github-actions"
    directory: "/"
    target-branch: "staging"
    schedule:
      interval: "monthly"
    labels:
      - "dependencies"
      - "ci"
