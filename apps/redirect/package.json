{
  "name": "@sparkmotion/redirect",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "secrets": "node -e 'const l=require(\"fs\").readFileSync(\".dev.vars\",\"utf8\").trim().split(\"\\n\").filter(x=>x&&!x.startsWith(\"#\"));const o={};l.forEach(x=>{const i=x.indexOf(\"=\");o[x.slice(0,i)]=x.slice(i+1)});process.stdout.write(JSON.stringify(o))' | wrangler secret bulk",
    "build": "echo 'wrangler handles bundling'"
  },
  "dependencies": {
    "@upstash/redis": "^1.34.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241218.0",
    "wrangler": "^4.69.0",
    "typescript": "^5.7.0"
  }
}
